# Flask / app
FLASK_SECRET_KEY=change-me-to-a-long-random-secret

# Email verification
EMAIL_VERIFY_MAX_AGE=86400
EMAIL_VERIFY_SALT=taskflow-email-verify

# Gmail SMTP
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-app-password

# Database
DB_HOST=your-database-host
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-db-password
DB_NAME=todo_list
DB_CONNECT_TIMEOUT=10
DB_CONNECT_RETRIES=5
DB_CONNECT_RETRY_DELAY=2
# Use false by default for low-traffic deployments to avoid stale pooled connections.
DB_USE_POOL=false
DB_POOL_SIZE=5

# Frontend / CORS
# Comma-separated list of allowed frontend origins. Regex patterns are supported by Flask-CORS.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://.*\.vercel\.app
# Set this to true when the frontend and Flask backend are on different HTTPS origins.
SESSION_COOKIE_CROSS_SITE=false
SESSION_COOKIE_SAMESITE=
SESSION_COOKIE_SECURE=

# Google OAuth
# Local redirect URI for this app:
# http://localhost:8001/auth/google/callback
# Optional explicit redirect URI for production:
# https://tflow.live/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=
